import styled from "styled-components";

export const Wrapper = styled.div`

    border: 1px solid #ccc;
    border-radius: 8px;
    width: inherit;
    height:inherit;
    padding: 12px;
    font-weight: 500;
`;
